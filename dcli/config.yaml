# dcli configuration pointer
# This file points to the active host configuration
# The full configuration lives in hosts/archmiku.yaml

# Active host
host: archmiku

# Package manager: pacman (Arch), apt (Debian/Ubuntu), or dnf (Fedora/RHEL)
package_manager: pacman
